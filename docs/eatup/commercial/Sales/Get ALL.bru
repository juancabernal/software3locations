meta {
  name: Get ALL
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/commercial/api/v1/sales
  body: none
  auth: bearer
}

auth:bearer {
  token: 
}

script:post-response {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("La respuesta es un array", function() {
    expect(res.getBody()).to.be.an('array');
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
